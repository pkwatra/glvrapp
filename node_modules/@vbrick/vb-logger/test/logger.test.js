'use strict';

var logger = require('../index');

describe('vb-logger', function () {

	describe('#info()', function () {
		it('should return without error', function (done) {
			logger.info('Log without error');
			done();
		});
	});
});